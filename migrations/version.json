{
  "migrations": {
    "0.1.0": [
      "CREATE TABLE IF NOT EXISTS discord_channel_agent_map (channel_id TEXT PRIMARY KEY, guild_id TEXT NOT NULL, agent_id TEXT NOT NULL, project TEXT, category_name TEXT, channel_name TEXT, created_at INTEGER NOT NULL, updated_at INTEGER NOT NULL);",
      "CREATE INDEX IF NOT EXISTS idx_dcam_agent ON discord_channel_agent_map(agent_id);"
    ]
  }
}
